select employee_id as NIK, first_name AS "Nama Depan" ,last_name AS Namabelakang, first_name||' '|| last_name AS "Nama Lengkap" from employees 
where first_name='Daniel'


select 
   employee_id,lower(first_name),le(last_name),salary,(salary/30) AS GAJI
   from 
       employees 


      select  salary from employees where salary > 10000 and employee_id > 200

           select  * from employees where salary > 10000 and employee_id > 200
 
 select employee_id,first_name,last_name,salary
 from
     employess
  where
  employee_id not in(16,163)
  and
  employee_id in(100)

  select *from employees 

where first_name like 'A%' OR last_NAME LIKE 'B%'


























